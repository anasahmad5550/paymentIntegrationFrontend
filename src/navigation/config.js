export const links = Object.freeze({
    root: "/",
    signup: "/signup",
    login: "/login"
});
  
export const routes = Object.freeze({
    root: links.root,
    signup: links.signup,
    login: links.login
});
  